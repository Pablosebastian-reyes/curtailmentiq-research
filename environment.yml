name: curtailmentiq
channels:
  - conda-forge
dependencies:
  - python=3.12
  - pandas
  - numpy
  - scikit-learn
  - xgboost
  - lightgbm
  - statsmodels
  - ruptures      # deteccion de puntos de cambio (Fase 6 de la revision)
  - matplotlib
  - jupyterlab
  - sqlalchemy
  - psycopg2
  - pyarrow
  - pip
  - pip:
      - pot          # Python Optimal Transport (Wasserstein)
      - mapie        # Conformal prediction
      - properscoring # CRPS
